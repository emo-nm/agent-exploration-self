export default function SmithersChildJob() {
  return (
    <main className="wrap">
      <a href="/">&larr; modes</a>
      <h1>Smithers child job</h1>
      <p className="muted">
        Phase 3. Framework-owned session (pattern B): the product agent launches
        a bounded Smithers child workflow via an allowlisted tool. Blocked until
        the direct baselines pass the durability suite (docs/plan.md gate).
      </p>
    </main>
  );
}

export default function SmithersCompare() {
  return (
    <main className="wrap">
      <a href="/">&larr; modes</a>
      <h1>Smithers compare</h1>
      <p className="muted">
        Phase 3. Smithers-owned run (pattern A): same prompt to Eve + Flue in
        parallel, blinded scoring. Blocked until the direct baselines pass the
        durability suite (docs/plan.md gate).
      </p>
    </main>
  );
}
